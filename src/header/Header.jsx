import Nav from "./Nav"
import style from "./Header.module.scss"

const Header = () => {
    return <header className={style.header}>
        <Nav />
        header
    </header>
}

export default Header